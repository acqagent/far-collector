# Core pipeline dependencies (FAR mode).
# vLLM (the inference server) is intentionally not listed — install it
# separately following https://docs.vllm.ai for your CUDA version.
beautifulsoup4>=4.12
duckdb>=0.10
httpx>=0.27
lxml>=5.0
openai>=1.40
openpyxl>=3.1
pydantic>=2.7
pypdf>=4.0
python-dateutil>=2.9
rich>=13.0
tenacity>=8.2
trafilatura>=1.8
