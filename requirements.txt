# Core pipeline dependencies (FAR mode).
# The inference server is intentionally not listed here — it is a separate
# install on the GPU box. See the README: the reference setup is SGLang +
# Qwen3.8-27B-NVFP4 + DSpark via https://github.com/acqagent/opencode-sglang.
beautifulsoup4>=4.12
duckdb>=0.10
httpx>=0.27
lxml>=5.0
openai>=1.40
openpyxl>=3.1
pydantic>=2.7
pypdf>=4.0
python-dateutil>=2.9
rich>=13.0
tenacity>=8.2
trafilatura>=1.8
